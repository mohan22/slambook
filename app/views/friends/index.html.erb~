<h1>Listing friends</h1>
<h1 align="justify">welcome to my slambook......</h1>
<table>
  <tr>
    <th>Name...</th>
    <th>Cell...</th>
    <th>Email...</th>
    <th>Dob..</th>
    <th>Aboutme  </th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @friends.each do |friend| %>
  <tr>
    <td><%= friend.name %></td>
    <td><%= friend.cell %></td>
    <td><%= friend.email %></td>
    <td><%= friend.DOB %></td>
    <td><%= friend.AboutME %></td>
    <td><%= link_to 'Show', friend %></td>
    <td><%= link_to 'Edit', edit_friend_path(friend) %></td>
    <td><%= link_to 'Destroy', friend, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Friend', new_friend_path %>
